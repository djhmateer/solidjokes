@model IEnumerable<Core.Models.Joke>

<table>
    @foreach (var item in Model) {
        <tr>
            <td>@item.Rating</td>
            <td>@if (!String.IsNullOrEmpty(item.Content)) { @item.Content }</td>
        </tr>
    }
</table>
